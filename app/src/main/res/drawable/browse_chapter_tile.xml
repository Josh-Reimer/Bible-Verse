<?xml version="1.0" encoding="utf-8"?>
<!-- One chapter number in the browse grid. Same fill/border as spinner_box, which is
     what the rest of the app uses for a tappable box, with a ripple on top. -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/spinner_fill" />
            <stroke
                android:width="1dp"
                android:color="@color/spinner_border" />
            <corners android:radius="8dp" />
        </shape>
    </item>
</ripple>
